(* program to average 100 integers *)     
program 123
var
	sum, _value, average,  : integer
begin
	sum := (0 + 0);
	average := 0 * ;
	for i := 1 to 100 do
		begin
			read(value);
			sum := sum + value;
		end; 
	average := sum div 100;
	write(average);
end.